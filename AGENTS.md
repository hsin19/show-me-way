# AGENTS.md

This repository is a Svelte 5 travel itinerary PWA. Agents should treat local skills as the primary workflow layer and use Svelte MCP CLI commands only when a skill or task calls for them.

## Project Overview

- App: ShowMeWay, a YAML-driven travel itinerary helper PWA.
- Stack: Svelte 5 runes, TypeScript, Vite, Tailwind CSS v4, `vite-plugin-pwa`, `js-yaml`, Vitest.
- Package manager: `pnpm`.
- Mount entry: `src/main.ts` mounts the root component `src/App.svelte`.
- User-facing language is primarily Traditional Chinese. Keep UI copy and validation errors consistent with that tone.

### Layering Rules (`src/lib/`)

The tree is cut on one axis: **what kind of thing a file is** — 算的 (`domain/`), 對外的 (`infra/`), 有狀態的 (`stores/`), 畫的 (`ui/`). A new file's home follows from that question alone; it is deliberately not cut by feature, so a subsystem like Drive sync is spread across `infra/http/gdrive.ts`, `stores/gdrive.svelte.ts` and `ui/tools/settings/`.

`domain/` is pure: no `fetch`, no svelte runtime, and no browser global **at import time**. That last qualifier is the whole rule — the payoff is that `domain/` is the answer to "can this be unit-tested", which matters here because there is no component test layer (see Testing Notes), so anything that ends up in a `.svelte` file has zero unit coverage by construction. A synchronous, stateless platform query guarded by `typeof window` is therefore legal in `domain/` and two exist on purpose: `prefersReducedMotion` (`domain/utils.ts`) and the `location`/`history` access in `domain/share.ts`, whose whole job is the URL fragment. Do not "fix" them by moving them; moving one without the other buys nothing, and the module-level rule they'd enforce is the import-time one, which already holds. Dependencies point inward, tests included: a `domain/` test that has to stub `localStorage` is testing something that lives in `infra/`.

`infra/` is split by **failure mode**, not by which API a file calls: `http/` is remote, async, someone else's contract, and dies offline; `storage/` is localStorage — synchronous, ours, bounded by quota; `pwa/` is a device/platform adapter. `weather.ts` is the worked example and the reason the axis is worth stating: it both fetches _and_ writes localStorage, and it lives in `http/` because what breaks it is being offline — the localStorage half is only its own cache. Classifying by "which API does it use" gives the wrong answer there, and twice it did: `share-import.ts` sat in `http/` because share links arrive from a URL though it makes no request at all, and `downloadTextFile` sat in `yaml-storage.ts` because YAML export was its first caller though it is a Blob/`<a download>` device adapter serving the ledger CSV too. Both now sit where the failure mode puts them (`storage/share-import.ts`, `pwa/file-download.ts`); ask what breaks a file, not what it calls.

`stores/` holds mutable app state — Svelte 5 runes singletons. Be aware the name undersells two of them: `gdrive.svelte.ts` and `trip.svelte.ts` are mostly _orchestration_ (sync decisions, profile switching, migration sequences), not state. That is the seam most likely to be stretched next.

`ui/` is split by screen: `itinerary/`, `tools/`, `tools/settings/` (sub-tabs hosted by `ToolsTab`), `ai/`, and `shared/` for what crosses screens — including shared UI `.ts` modules, which is why `edge-fade.ts` and `enlarge.ts` live there rather than in `domain/`. `shared/` means _generic leaf_, not merely "used twice": a component with domain knowledge stays with its canonical host even when a second screen borrows it. `ProfileManager` is the one such case — its home is 行程管理, and `TripOverview`'s drawer is a shortcut entry. Apart from it, `ui/` contains no page-to-page imports.

Two cross-cutting exceptions the layering does not govern. `showToast` is an app-global sink that **any** layer calls directly — `stores/toast.svelte.ts` says so at the top, and the point is to spare every layer in between an `onToast` prop; `infra/pwa/sw-update.ts` importing it is therefore intended, not an inverted edge. And `ui/tools/settings/SettingsPanel.svelte` writes `showmeway_user_yaml` directly (`:252`, `:433`) rather than going through `tripStore`, so that key has two writers and the sequence around it — `backupCurrentYaml` → write → clear `settingsDraft.yaml` → `onReload()` → tell `gdriveSync` — has no single owner. Any new whole-document write must reproduce it by hand; the Data Model section records what it cost the last time a copy of that flow drifted.

### Key Modules (`src/lib/`)

- `domain/`: Pure domain calculation and utilities (`trip.ts`, `trip-schema.ts` — the authored itinerary shape as a valibot schema, `trip-issues.ts` — its zh-TW messages, `timeline.ts`, `ledger.ts`, `phrases.ts`, `markdown.ts`, `diff.ts`, `share.ts`, `share-crypto.ts`, `utils.ts`).
- `stores/`: Reactive Svelte 5 runes stores (`trip.svelte.ts`, `weather.svelte.ts`, `toast.svelte.ts`, `theme.svelte.ts`, `gdrive.svelte.ts`, `share-link.svelte.ts`, `gemini-models.svelte.ts`, `pwa-install.svelte.ts`, `settings-draft.svelte.ts`).
- `infra/storage/`: Storage adapters & persistence (`yaml-storage.ts`, `profiles.ts`, `share-import.ts`, `share-links.ts`, `storage-admin.ts`, `storage-cache.ts`).
- `infra/http/`: External service clients (`gdrive.ts`, `gemini.ts`, `weather.ts`, `exchange.ts`, `hop.ts` with `share-link.ts` as the one place a parsed share link becomes YAML) plus `itinerary-loader.ts` (fetches the bundled default itinerary — same-origin static files, not a third-party service — and layers `showmeway_user_yaml` on top; still infra/http/-shaped because the fetch dies offline).
- `infra/pwa/`: Browser hardware & PWA adapters (`sw-update.ts`, `wakelock.ts`, `file-download.ts`).

### Key UI Modules & Components (`src/lib/ui/`)

- `itinerary/`: `ItineraryStrip.svelte` (composition root), `TripOverview.svelte`, `Timeline.svelte` (dynamic timeline), `HotelCards.svelte`, `EnlargedCardOverlay.svelte`, `DayChip.svelte`, `WeatherBadge.svelte`.
- `tools/`: `ToolsTab.svelte` (sub-tabs host), `Checklist.svelte` (packing & todo), `Ledger.svelte` (currency & expenses), `PhraseDeck.svelte` (offline phrases), and `settings/` (`SettingsPanel.svelte`, `AppSettings.svelte`, `ProfileManager.svelte`).
- `ai/`: `ChatPanel.svelte`, `DiffView.svelte` (AI assistant chat, diff inspection, and itinerary edit application).
- `shared/`: `TabPager.svelte` (shared swipe/chip pager), `ConfirmBar.svelte`, `RichText.svelte` (safe AST Markdown renderer), `Toast.svelte`, `edge-fade.ts`, `enlarge.ts`.

## Core Invariants & Guardrails

1. **LocalStorage & Storage Scoping**:
   - Every localStorage key written by this app MUST start with `showmeway_` (or `exchange_rate_` for legacy Ledger).
   - **NEVER use `localStorage.clear()`**: ShowMeWay is deployed on GitHub Pages (`*.github.io`), sharing the origin with other user projects. Always use `storage-admin.ts` (`clearAppLocalStorage()`) for scoped deletion.
2. **Dynamic UI Nodes vs YAML Persistence**:
   - **Never author or persist「退房」or「回飯店休息」in YAML**: These timeline nodes are synthesized dynamically in `Timeline.svelte` from `trip.hotels`. Persisting them creates duplicate undeletable events.
   - `trip.start`, `trip.end`, `trip.departure`, and `days[].day` are **derived, never authored**. `normalizeTripData` calculates them automatically and `serializeToYaml` strips them upon saving. `trip.id` is also generated there and also never authored, but is **kept** on save — it is the trip's identity, not a restatement of its contents. See the Data Model section.
3. **Privacy & URL Handling**:
   - The inline share link MUST place the compressed payload in the **URL fragment (`#s=...`)**, NEVER in a query parameter (`?s=...`), so itinerary data is never transmitted to the CDN's access logs.
   - The short link (`#h=<id>.<key>`) uploads **AES-GCM ciphertext and nothing else** to `hop.hsin19.com`. The key MUST stay in the fragment for the same reason, and MUST NEVER be sent to hop, logged, or persisted anywhere **off the device** next to the id — one log line holding both is a plaintext itinerary. `sealShareToken` therefore generates its own key and IV and accepts neither as an argument; `resealShareToken` is the one deliberate exception (an existing key, always a fresh IV) and exists only so a link can be updated in place. The sender's own device stores id, key and hop's `editToken` together in `showmeway_share_links` (`infra/storage/share-links.ts`): that device already holds the plaintext, so the pair adds no exposure there, and it is what makes the same link updatable at all. The `editToken` is a bearer secret for overwriting or deleting the ciphertext — it may go back to hop, and must never enter the YAML that gets exported, shared or sent to Gemini.
   - hop is the app's **only first-party upload**, so it stays behind an explicit user action and is disclosed in `public/privacy.html`, in the README, and in the share toasts on both the clipboard and the share-sheet path. Never make it the silent default for an action that today reaches no server, and never let a failure to reach hop clear the URL hash — the address bar holds the only copy of the key on that device. `resolveShareLink` (`infra/http/share-link.ts`) is the only place that classifies a share-link failure: a `ShareLinkError` with `retryable` set means "keep the link", and `fetchHopBlob` reports `gone` only for 404/410 because that is the value that clears the hash (see `maybeImportSharedItinerary`, pinned by `trip.svelte.test.ts`).
4. **Security & Content Rendering**:
   - **Never use `{@html}` in `RichText.svelte`**: Shared itineraries come from external sources. All links and formatting must be parsed into an AST and validated via `sanitizeHref` / `sanitizeLinkHref` allowlists.
5. **AI Itinerary Updates**:
   - The AI assistant outputs the full updated YAML via `update_itinerary`. Updates must always pass `validateYaml` and manual user confirmation via `DiffView` before calling `applyAiEdit`.

## Skills-First Svelte Workflow

When creating, editing, or analyzing `.svelte`, `.svelte.ts`, or `.svelte.js` files, use the local Svelte skills first:

- `.agents/skills/svelte-code-writer`
- `.agents/skills/svelte-core-bestpractices`

These skills define the preferred Svelte 5 workflow. Before finalizing Svelte component edits, run the project checks listed below.

## Commands

- Install dependencies: `pnpm install`
- Start dev server: `pnpm dev` (fixed port 8045; `vite preview` is 8046 — both `strictPort` in `vite.config.ts`). `localStorage` is keyed by host:port, so a saved trip only exists on those origins. On `EADDRINUSE`, free the port instead of passing `--port`: on a fresh origin the app silently falls back to the bundled template and looks like it lost the user's data.
- Verify everything: `pnpm run check`
- E2E tests: `pnpm run test:e2e` — also run by `check`, and standalone when you want only this layer. The `webServer` builds and serves dist via `vite preview` on port 8046; `pnpm install` does not download browsers, so run `pnpm exec playwright install chromium` once per machine.

`pnpm run check` is the one to run. It chains schema generation, format, knip, lint, typecheck, unit tests, build and e2e and names the step that failed, so there is no reason to invoke those individually or to list them here. It is deliberately the same set `.github/workflows/check.yml` runs in CI — keep that equivalence when adding a check, or one of the two silently stops being the gate. **It repairs what it can rather than just reporting** — `schema:gen`, `dprint fmt`, `knip --fix`, `eslint --fix` — so expect a clean run to have edited your files. Five things its output will not tell you:

- Its first step is `schema:gen`, which rewrites `schema/showmeway-schema.json` from `src/lib/domain/trip-schema.ts` (see the Data Model section); the JSON is a build product that happens to be committed, so never hand-edit it.
- `dprint fmt` comes next and rewrites files across the repo, not just the ones you touched — on a tree carrying unformatted edits you did not author, it folds them into your diff, against the Git section's rule about preserving unrelated working-tree changes.
- `knip:fix` strips the `export` keyword off anything nothing imports — it never deletes the declaration, and it is pinned to `--fix-type exports,types` so it cannot edit package.json or remove files. It runs **before** `lint:fix` on purpose: `no-unused-vars` treats an `export` as public API and goes silent, so stripping the keyword first is what lets ESLint report a genuinely dead symbol in the same run. To keep an export knip would strip, tag it `/** @public */`.
- Because those two chain, a `check` run can both edit a file and then fail on it: knip removes the `export`, ESLint reports the now-file-local symbol as unused, and deleting it is yours to do. That is the intended flow, not a conflict.
- Its e2e step sets `E2E_SKIP_BUILD=1`, which makes Playwright's `webServer` serve the `dist/` the preceding `build` step just produced instead of building a second time (16s of the chain). The flag is safe **only** because it sits on the line after `build`; setting it anywhere else tests a stale `dist/`. `test:e2e` on its own leaves it unset and builds normally.

**CI does not run `check`, or any single script standing in for it.** `.github/workflows/check.yml` — the shared gate that both `pr.yml` and `deploy.yml` call, so a PR and a `main` push cannot be held to different bars — runs the non-writing counterparts as one named step each: `format:check`, `schema:check`, `lint`, `typecheck`, `knip:ci`, `test:ci`, `build`. One step per check is the point: the Actions UI names the check that failed instead of showing one opaque red step, and `!cancelled()` on each lets a single run surface _every_ failure rather than stopping at the first. Adding a check to CI therefore means adding a step there, not editing a script.

`pnpm run ci` is checks-through-to-build in one command: `check` minus the repairing (`schema:check`/`format:check`/`lint`/`knip:ci` instead of their writing counterparts) and minus e2e. **Despite the name, GitHub Actions does not run it** — `check.yml` spells those steps out instead, deliberately, so the Actions UI can name the one that failed. Its actual consumer is the build command of the **Cloudflare Pages** project built from this repo, which wants a single non-repairing command and has no browsers. It runs plain `test`, not `test:ci`, because nothing there consumes a coverage or JUnit report and `test:ci`'s `github-actions` reporter would only litter that build log. That caller lives outside the repo where no grep will find it, so the script has zero in-repo callers and still is not dead — do not remove it, and do not "simplify" `check.yml` into a call to it.

`test:ci` is `test` plus a v8 coverage report over `src/lib/**/*.ts`, a JUnit file (both uploaded to Codecov) and the `github-actions` reporter — vitest only auto-enables that last one when NO reporter is configured, so passing `--reporter=junit` silently costs you the inline annotations on failing tests unless it is listed too. `knip:ci` drops the dependency checks: those flip when an upstream package restructures its own dependencies, which must not gate a Dependabot bump. See `knip.jsonc`. e2e is a second **job** in `check.yml`, not a step in the check job: it runs in parallel with the rest, and a failure names `e2e` rather than hiding inside a long step list.

## Data Model

Itinerary data is YAML. Loading priority:

1. User YAML saved in `localStorage` under `showmeway_user_yaml`.
2. `public/itinerary.local.yaml` for personal local data. This should remain untracked.
3. `public/itinerary.yaml` as the default template.

A share link — inline `#s=<token>` or short `#h=<id>.<key>` — short-circuits that list: `App.svelte`'s `onMount` runs `tripStore.maybeImportSharedItinerary()` before the first load, which resolves the link to YAML through `resolveShareLink` (a hop fetch plus decrypt for the short form) and writes it into `showmeway_user_yaml` (source 1) rather than adding a fourth source. Which write depends on whether the incoming `trip.id` names a trip this device already holds (`findProfileByTripId`). If it does not — the ordinary case, someone else's trip — it goes through `createProfile`, which parks the current trip as a profile instead of overwriting it, after a confirm unless there is no existing trip. If it does, the link is a newer version of a trip already here, so the first offer is to **replace** that copy: it switches to that profile, snapshots it into the backup ring, and writes over it, keeping the id and therefore the Drive binding. Declining that offers a copy instead (`ensureUniqueTripId` gives it a fresh identity, or the two would fight over one Drive file), and declining both does nothing. The hash is stripped afterwards on every outcome but one — success, decline, a corrupt or expired link — so a refresh cannot re-prompt. The exception is a **retryable** failure to resolve a short link (offline, hop down, a browser without SubtleCrypto): the hash is the only copy of the key on that device, so it stays put and the toast says to try again. `trip.svelte.test.ts` pins that split.

All of that branching lives in `importSharedTrip` (`src/lib/infra/storage/share-import.ts`), not in the store that calls it, because the hash is not the only way a link arrives: pasting one into 行程管理's YAML editor is a documented import path too, and that copy of the flow used to be a plain write into the active slot — which left the incoming trip wearing the outgoing one's Drive binding and PATCHed a stranger's cloud file on the next sync. Anything keyed by trip must use the `profileId` the outcome reports, never an id captured before the call: an import moves the active slot.

The authored shape is declared once, as a valibot schema in `src/lib/domain/trip-schema.ts`, and three things derive from it so they cannot drift: the runtime gate (`normalizeTripData` parses with `itinerarySchema` — the only real gate, since nothing validates against the JSON at runtime), the TypeScript types (`ChecklistItem`, `HotelInfo`, `TimelineEvent`, … are `InferOutput`s of it; `trip.ts` layers the derived fields and `_id` on top), and the editor's `schema/showmeway-schema.json`, generated by `scripts/gen-schema.ts` — `pnpm run schema:gen` writes it (first step of `check`, and the pre-commit hook re-runs it when `trip-schema.ts` is staged), `schema:check` fails CI when it is stale. The JSON lives outside `public/` and is committed rather than built: the app never reads it, editors fetch it through the modeline's GitHub raw URL (`SCHEMA_LINE` in `trip.ts`, which therefore follows `main` without a deploy and survives a hosting move), and the yaml-builder skill's validator reads the checkout copy. The generator runs under plain `node` (Node 24 strips types natively, hence the `.ts` extension on its import) and is typechecked by `tsconfig.node.json`. A new field therefore lands in three places: the schema, with a `v.description` written for an author — the itinerary-yaml-builder skill points at the generated JSON instead of keeping its own field table, so that description _is_ the documentation and a field without one is a field nobody writes; a case in `src/lib/domain/trip.test.ts`; and the example `public/itinerary.yaml`. `v.metadata` carries editor-only hints (`deprecated`, `readOnly`, `maxLength`, `enum`) the app deliberately does not enforce — `lang` accepts any string because `phrases.ts` falls back to English, a long title wraps rather than failing — and the generator spreads them into the JSON verbatim, adding `additionalProperties: false` to every object because the runtime strips unknown keys (next paragraph) and the editor is the only place a typo gets pointed out. Messages stay zh-TW through `trip-issues.ts`, which maps the first issue's path onto the wording `trip.test.ts` pins, spacing quirks included (`trip的 id`, `confirmation 的 code`) — change wording there, not in the schema. Two things the schema deliberately leaves to `trip.ts`: the `null` a bare `desc:` parses to is deleted before parsing so it reads as "absent" everywhere (blank for optional fields — `desc` and a checklist `text` have always rendered as a blank line, which is why they are optional — and "missing" for required ones), and the cross-field checks after it (a date the calendar does not have, two days on one date, a gap over `MAX_DAY_GAP`), whose messages talk about dates rather than types. `stops` (f728a17) is the worked example of adding a field. A field that also renders through `RichText` (joins `desc`, `bullets`, `alternatives`' `note`, and a checklist `text`) needs a fourth place: rule 8 in `buildSystemInstruction` (`src/lib/infra/http/gemini.ts`), which hardcodes that same field list to tell the AI which fields accept Markdown — nothing keeps it in sync, so a field added elsewhere without it just makes the AI's Markdown advice quietly wrong for that field. A change to the _derivation_ rules (what counts as deprecated/authored, how `days[].day` or the trip's date range are computed) also has to reach `.agents/skills/itinerary-yaml-builder/scripts/validate_itinerary.py`: its schema-level check stays correct automatically once the schema does, but its custom semantic checks (day-numbering, trip/hotel date-boundary cross-checks) hardcode those derivation rules independently in Python and drifted once before — it rejected this repo's own `public/itinerary.yaml` by requiring the `deprecated` `day` field to be present and treating a skipped date as an error, contradicting `normalizeTripData`'s actual gap-filling behavior. Run it (`uv run .../validate_itinerary.py public/itinerary.yaml public/itinerary.local.yaml`) after any such change.

`serializeToYaml` strips runtime-only `_id` values (timeline events, checklist items, and expense records) and re-adds the YAML schema modeline. Do not persist `_id` into YAML fixtures or exports. It clones via `JSON.parse(JSON.stringify(...))` deliberately — `structuredClone` throws on the `$state` proxy it is handed, and swapping it in passes every unit test (those pass plain objects) while breaking every save in the browser. Saving is canonicalization, not a round-trip: the js-yaml dump preserves array order but not comments or hand-authored key order, and `validateYaml` parses through `itinerarySchema`, whose objects strip every key they do not declare, so an unknown key at any depth — a top-level `notes:`, a hotel's `phone:`, a misspelt `mapLnk:` — is silently dropped on the next save; the generated JSON Schema's `additionalProperties: false` is what shows it in the editor. Key order is canonical too (schema order, `id` right after `name`, `pace` last in a day), so the first save after an upgrade rewrites hand-authored order and the Drive record reads as locally changed once. This is not new with any one editor — every save path re-serializes, including a checklist toggle.

`trip.start`, `trip.end`, `trip.departure` and `days[].day` are **derived, never authored**: `normalizeTripData` sorts `days` by `date`, numbers them, fills a skipped date in as a 自由活動 free day, takes the first/last dates as the range, and derives `departure` from day 1's first event time (offset-free local time — the countdown targets the first event, not a flight). `serializeToYaml` strips all four, so a hand-written value survives exactly until the next save. They stay **required** on `TripData["trip"]` because a loaded trip always has them; the schema keeps them as `deprecated` so old files do not light up in an editor. That invariant — `departure` always sits on `start` — is what makes `getCountdownText`'s "already left" case unreachable, hence absent.

`trip.id` is the near-miss of that group, and the exception worth stating: also minted by `normalizeTripData` and never authored, but deliberately **not** stripped on save. It is the trip's identity rather than a restatement of its contents, so it travels with the export, the share link and the Drive copy — `appProperties.showmewayTripId` is this value, which is what lets a device that lost its `showmeway_gdrive_trips` map work out which cloud file is this trip. Strip it and the next load mints a new one, orphaning that file. Being minted only when absent makes every whole-document rewrite a hazard: `applyAiEdit` restores it from the trip on screen (rule 11 in `buildSystemInstruction` only saves the model the trouble of inventing one), and the share import branches on it entirely (see the share-link paragraph above). For the same reason it is absent from `public/itinerary.yaml` and marked `readOnly` in the schema instead of being listed as an authorable field — nobody writes it. `TripStore.load()` (`src/lib/stores/trip.svelte.ts`) persists it once for a trip stored before ids existed (an identity that changes every launch is worse than none); that one rewrite makes every pre-existing sync record read as locally dirty, so the first sync after the upgrade pushes — or asks, if Drive moved too. `e2e/tests/fixtures.ts` seeds an id for exactly that reason.

The Drive binding (trip → file id, in `showmeway_gdrive_trips`) is a **rebuildable cache, not a record of truth**: `reconcileBindings` runs after every successful listing and re-derives a lost binding by matching each file's `appProperties.showmewayTripId` against the local trips' `trip.id`. That is what makes a sign-out, a reinstall, or an evicted `localStorage` recoverable instead of producing a duplicate cloud file. It only ever fills gaps — an existing record, or a file another trip already claims, is left alone — and it binds silently only when `appProperties.contentHash` proves the two copies identical. When they differ it still records the binding (so nothing creates a duplicate and the resolution has a file to act on) but writes **no** `localHash` and sets `diverged` on the record instead. That flag is what holds the trip, and it is on the _record_ rather than in UI state on purpose: a missing `localHash` reads as "local moved" and decides `push` all by itself, so a conflict kept only in memory stopped holding the trip at the next reload. `decideSyncAction` refuses to push past `diverged` — only the two sides turning out to hold identical content settles it without a decision, and `adoptCloudTrip` clears it by writing a whole new record. `conflictFor` reports a `diverged` record as a live conflict so the decision strip survives a restart too.

A conflict changes nothing on either side when raised; it pauses automatic sync for that trip and replaces the cloud button with `SettingsPanel`'s decision strip. Three ways out, each behind its own confirm: 採用雲端版本 (`force: "remote"`, backed up first), 保留本機版本 (`force: "local"` — the one action in the app that is **not** recoverable from inside it, since it overwrites the Drive file's contents), and 兩份都留, which appears only for `both-changed` because `remote-newer` means this device changed nothing worth keeping. That third path pulls the cloud copy into the trip's existing slot — keeping its id, and so its Drive binding — and hands the copy that was there to `branchLocalCopy`, which re-identifies and renames it into a trip of its own. Its order is load-bearing: the local YAML is read out of storage before the pull overwrites it, and `landPulled` returning false (invalid download, or a trip switched out from under the round trip) has to abort the branch rather than fork off a second trip from bytes that never landed.

A `pulled` result is **not** yet recorded: `sync` hands back the YAML plus a `commit` thunk, and every caller must call it only once those bytes are persisted (`if (await landPulled(res.yaml)) res.commit?.()`). Recording ahead of the caller claims this device holds a version it never took, and the next sync then pushes the older local content over the newer cloud copy without raising anything. The same thunk is what clears the conflict and toasts, so a pull nobody could land leaves the decision strip up instead of looking settled.

`sync({ checkOnly: true })` — the 同步 button, labelled 比對 — transfers nothing in **either** direction; it only arms `pendingTransfer` so the button turns into an explicit 上傳/下載 tap. The push half matters as much as the pull half: a locally clean trip decides `push` exactly when its Drive file has gone, and re-creating a file the user deleted from Drive is not something a compare button may do on its own.

AI chat edits are the one whole-document write the user never authors: the model calls `update_itinerary` with the **complete** updated YAML, `ChatPanel` validates it and gates it behind a `DiffView` confirm, and `applyAiEdit` (`src/lib/stores/trip.svelte.ts`) revalidates, calls `backupCurrentYaml` (the only undo), then assigns `this.data` in place — not via `TripStore.load()`, which would unmount the AI tab and lose its conversation. Because the model re-authors the whole file while `normalizeTripData` defaults `todo`/`packing`/`expenses` to `[]`, a section the model forgets to echo back validates clean and is silently wiped; a new top-level section has to be named in `buildSystemInstruction`'s edit rules, not just added to `TripData`. It is also the only path that regenerates every `_id` without remounting, so anything keyed by `_id` (a pending undo, say) goes stale in place.

Trip profiles (multiple trips): the active trip stays in `showmeway_user_yaml`; the other trips are parked as YAML snapshots in `showmeway_profiles`, with `showmeway_active_profile` holding the active id (see the profile helpers in `src/lib/infra/storage/profiles.ts`). Switching swaps the chosen snapshot with the active one — a separate, user-managed list (never auto-evicted, unlike the backup ring). The switcher UI is `ProfileManager`, hosted both at the top of 行程管理 and in TripOverview's hero drawer; switching navigates back to the 行程 tab. Only the itinerary YAML travels with a profile. Any caller that swaps the active YAML must also clear `settingsDraft.yaml` — a draft left from the previous trip outranks the persisted YAML in the editor and would be saved over the newly active trip.

Where the YAML leaves the device the split is by audience, not by medium: only `shareCurrentTrip` (the overview's 分享行程 link, also the 分享連結 section of 行程管理) serializes with `expenses: []`; the file export, Drive sync and the Gemini grounding context (`buildItineraryContext` is just `serializeToYaml(tripData)`) all ship the full trip. There used to be a second link path — a 含記帳 cross-device transfer link — and it was removed once Drive sync covered moving one's own data; do not bring it back as a shortcut, and make the expenses choice explicitly for any new personal field. The share link is **persistent and per trip**: `shareLinks.publish` (`stores/share-link.svelte.ts`) keys a record by profile slot, like the Drive binding, and on every tap after the first re-encrypts under the stored key and `PUT`s to the stored id with a one-year TTL, so a printed QR keeps resolving to the newest version. Its outcomes are load-bearing for the toasts: `updated` keeps the URL, `recreated` means hop reported the old id gone or the token rejected (the printed QR is dead and the user is told), `inline` is the no-crypto or first-upload-failed fallback to `#s=`, and `unreachable` hands out **nothing** — a fresh id would split the audience and an inline link would differ from the one already sent around. `revoke` deletes the blob; `deleteProfile` only forgets the record, leaving the recipients' copy to expire. On the receiving side `importSharedTrip` short-circuits to `unchanged` when the link's canonical YAML equals the copy already held, because reopening a persistent link to check for updates is the common case and an overwrite prompt for identical bytes teaches dismissal. Every path that uploads says so in its toast and `public/privacy.html` documents it — that disclosure is part of the feature, not decoration.

Other `localStorage` keys exist outside the itinerary YAML: `exchange_rate_<currency>` (`Ledger`'s working rate — despite `MANUAL_RATE_KEY_PREFIX`'s name, the user's manual override and the last auto-fetched rate share this one key, so a resolved fetch overwrites the manual value on the next load), `showmeway_exchange_rates_<base>` (rate cache in `src/lib/infra/http/exchange.ts`), `showmeway_yaml_backups` (auto-snapshots of the user YAML taken before each destructive overwrite — newest first, max 5; see `backupCurrentYaml` in `src/lib/infra/storage/yaml-storage.ts`), `showmeway_geocode_v1_<city>` / `showmeway_weather_<city>` (weather cache in `src/lib/infra/http/weather.ts`; geocode entries carry a 30-day TTL — no longer permanent — and forecasts a 3h TTL), `showmeway_gemini_api_key` / `showmeway_gemini_model` / `showmeway_gemini_model_filter` (the user's own Gemini API key, the chat model they picked from the dynamically fetched list, and whether that list is filtered to clean release names or left unfiltered — `default` / `all`, absent means `default`; managed by `src/lib/infra/http/gemini.ts` / `AppSettings.svelte` / `ChatPanel.svelte`, never written into the itinerary YAML and not carried with a profile), `showmeway_theme` (light/dark preference, `src/lib/stores/theme.svelte.ts` — app-level, so it is absent until the user picks one and never travels with a profile), and `showmeway_pwa_install_dismissed` (a timestamp; `src/lib/stores/pwa-install.svelte.ts` stops re-raising the install offer for 7 days after it. Absent until the offer is first declined or ignored, and app-level — an install is a property of the device, not of a trip. `e2e/tests/fixtures.ts` pre-stamps it so the 3.5s toast never lands mid-test). Checklist checked-state AND ledger expense records (`TripData.expenses`) live inside the itinerary YAML itself. Nothing reads the `todo_state` / `packing_state` / `ledger_expenses` keys any more — the one-time migration that folded them into the YAML has been retired — and they survive only in `storage-admin.ts`'s sweep list so a stray copy can still be cleared; do not reintroduce them.

That list is not maintained as an inventory — it explains the keys whose _behaviour_ is surprising, not every key that exists. For what is actually stored right now, read `storage-admin.ts`'s prefix sweep or open 本機儲存與快取 in App 設定; a new key needs no entry here unless something about it would mislead a reader (Google Drive's, for instance, are ordinary per-trip records and get none).

Each of those keys is declared exactly once in TypeScript, in the module that owns it (`YAML_BACKUPS_KEY` in `yaml-storage.ts`, `MANUAL_RATE_KEY_PREFIX` in `ledger.ts`, `GEMINI_*_STORAGE` in `gemini.ts`, `THEME_KEY` in `theme.svelte.ts`, the cache prefixes privately inside `weather.ts` / `exchange.ts`); the one literal outside a module is `showmeway_theme` in `index.html`'s pre-paint script, which runs before any import can — rename it in both places. Code that needs to enumerate or clear someone else's keys asks that module for a `*Keys()` / `clear*()` function instead of restating the string — see `storage-admin.ts`.

## Svelte And UI Guidelines

- Use Svelte 5 runes for new component code: `$state`, `$derived`, `$derived.by`, `$effect` only when appropriate.
- Prefer event attributes such as `onclick` over legacy `on:click`.
- Use keyed `{#each}` blocks with stable keys. Do not use indexes as keys when item identity exists.
- Keep component state local unless there is a concrete need to share it.
- Use `@lucide/svelte` icons for interface actions when an icon exists, always via the per-icon deep path (`import Calendar from "@lucide/svelte/icons/calendar";`) — every icon import in the repo does. The barrel form passes lint, typecheck and build and tree-shakes to a byte-identical bundle, so nothing in `pnpm run check` flags it, but it drags the whole icon set through the Svelte compiler (module graph 301 → 3931) and slows the dev server and every build.
- Keep mobile ergonomics in mind; this app is meant to be installed and used on a phone while traveling.
- `src/app.css`'s `standalone` variant (`@media (display-mode: standalone)`) exists solely for the app shell's `h-dvh standalone:h-screen` in `App.svelte`. It looks redundant and is not: WebKit computes `dvh` with phantom browser chrome on an installed-PWA cold start, while `100vh` is exact there. Removing either half — or the seemingly unused variant — breaks the installed iOS PWA, and no browser tab or Playwright run can reproduce it.
- Avoid introducing large visual redesigns while making functional changes unless explicitly requested.

## Design Tokens And Color

All color lives in `src/app.css`, where each token carries a comment on its role: the `@theme` block holds the dark palette and generates the Tailwind utilities; `:root[data-theme="light"]` redeclares every token whose meaning is theme-dependent (all but `scrim`), un-layered so it outranks Tailwind's `@layer theme`. A new color needs both entries or it silently renders its dark value on paper — nothing checks that the two lists match. Components must contain **zero** literal colors — no hex, no raw Tailwind palette (`slate-700`), and no `white/N` / `black/N`: `bg-white/5` would hardcode "lighter = add white", which inverts on a light background.

`data-theme` on `<html>` is the only switch. `src/lib/stores/theme.svelte.ts` owns the preference (`system` / `dark` / `light`, default `system`) and keeps every `theme-color` meta in step; an inline classic script in `index.html` resolves the same thing before first paint, because a module script would flash the wrong theme. **Those two copies of the resolution logic must stay in sync** — `e2e/tests/theme.spec.ts` is what catches it if they drift. That spec guards more than the pair: it also recomputes contrast for eleven token/backdrop pairs in **both** themes and fails anything under 4.5:1 — including `accent` on its own `bg-accent/15` chip fill — so a palette edit has to clear AA before it lands. It also pins `--color-scrim` to its opaque built form, because the driver-facing blackout must not follow the theme.

Opacity modifiers on tokens are fine (`bg-accent/15`, `hover:border-positive/50`): they `color-mix` a `var(--color-*)`, so they still flip with the theme. Only `white`/`black` literals are banned.

Besides the semantic colors (`accent`, `booked`/`must`/`option`, `positive`/`danger`, `bg-main`, `card-bg`, `text-*`) there are translucent ramps for surfaces stacked on other surfaces: `tint-1/2/3` (lift), `line-faint`/`line`/`line-raised`/`line-emphasis` (edges), `well`/`well-deep` (recess), plus `scrim`, `lift`, and `.panel`'s `--panel-shadow`. Two more are conventions rather than choices: `accent-contrast` is the foreground on every solid `accent` fill (it is what replaces the banned `white` literal), and `card-border` is the default border for cards, inputs, and quiet chrome — so a new control takes `border-card-border`, not one of the `line-*` ramps.

Changing `bg-main` also means updating `<meta name="theme-color">` in `index.html`, the manifest colors in `vite.config.ts`, and `:root`'s `color-scheme`.

## Formatting And Style

- Formatting is handled by dprint (config `.dprint.jsonc`) with 4-space indentation for TypeScript/Svelte-related files and 2-space indentation for YAML/JSON. Imports get rewritten every time: declarations and named specifiers sort case-insensitively, and any import with two or more specifiers is forced one-per-line, so hand-ordering them is wasted work. `lineWidth` is 1000 — long single lines are normal here, and dprint neither breaks them nor unwraps expressions you wrapped by hand.
- TypeScript uses double quotes and semicolons.
- YAML prefers single quotes — but `public/itinerary.yaml` and `public/itinerary.local.yaml` are in dprint's `excludes`, and dprint honors that even for an explicitly passed path, so `format`, `format:check`, and the pre-commit hook all silently skip them. Match the fixture's existing style by hand; it is what `serializeToYaml` emits — single quotes, no line folding.
- Comments explain **why**, never **what**. A comment that restates the code it sits on is noise, and usually a sign the code should have been named better instead — rename the function or extract the expression rather than captioning it. Write one when the reason is not recoverable from reading the code: a browser or API quirk being worked around, an ordering or invariant a future edit would silently break, an approach that was tried and rejected, a magic number's source. Section headers (`// --- helpers ---`), banner comments over an obvious block, restating the signature above a function, and narrating each step of a sequence all count as noise — delete them.
- Doc comments (`/** … */`) describe the **interface**, not the implementation: what the export is for and what a caller is bound by (throws vs. returns null, a required call order, a mutation, a unit, a caveat). How it achieves that — the algorithm, the workaround, the reason for a particular branch — is a body comment; a caller reading the doc should not learn things that a rewrite of the body would invalidate. Write one only where the name does not already carry the contract, and never restate parameter names as `@param`.
- A comment must age with the code it explains. If a comment names a file, function, or behavior, an edit that moves any of those has to update or delete it — a confidently wrong comment costs more than none.

## Testing Notes

- Vitest runs in `environment: "node"` with `include: ["src/**/*.test.ts"]` (`vitest.config.ts`) — no jsdom, no `@testing-library`, hence **no component-test layer**: `.svelte` components are covered only end-to-end by Playwright. Logic that needs unit coverage must live in a `src/lib/` module with a sibling `*.test.ts`; `.svelte.ts` rune modules count, and are why the config still loads the svelte plugin. No browser globals exist there, so no module may touch `window` / `localStorage` / `fetch` at import time — reach them inside functions only, and stub with `vi.stubGlobal` in tests — `src/lib/testing/stubs.ts` holds the shared `localStorage` and `window` timer stubs, and is excluded from coverage so it never counts as app code. A test named `*.spec.ts`, or placed outside `src/`, silently never runs.
- `vitest.config.ts` is standalone, so `vite.config.ts`'s `define` block is absent: `__APP_VERSION__` / `__BUILD_TIME__` are declared in `src/vite-env.d.ts`, which keeps typecheck and build clean while an unguarded read is a `ReferenceError` under `pnpm run test` — guard with `typeof … !== "undefined"` the way `version.ts` does.
- Module-level state outlives each test in a file. `toast.svelte.test.ts` drains pending timers and dismisses survivors in `afterEach`; `pwa-install.svelte.test.ts` rebuilds the module graph per test with `vi.resetModules()` + `await import(...)` because its session latches are deliberately unresettable — and it re-imports `toast.svelte` from that same fresh graph, or assertions read a different store instance than the code under test writes to.
- Pure date/time helpers belong in `src/lib/domain/utils.ts` and should have Vitest coverage in `src/lib/domain/utils.test.ts`.
- Ledger pure calculations belong in `src/lib/domain/ledger.ts` (covered by `src/lib/domain/ledger.test.ts`); `Ledger.svelte` is a controlled component — expense records come in as the `expenses` prop (owned by `TripStore`, persisted in the itinerary YAML) and add/delete/reset go back through callbacks. It keeps only its own input `$state` plus the manual exchange-rate localStorage, and wraps the pure functions in `$derived`.
- Be careful with `YYYY-MM-DD` parsing. This project intentionally parses plain dates in local time to avoid UTC day shifts.
- PWA, Vite and asset behavior break at build time and nowhere else — no unit test reaches them — so `pnpm run check` (which builds) is the only thing that catches a regression there.
- Playwright e2e lives in `e2e/tests/` (11 specs, config `playwright.config.ts`) and is more than `smoke.spec.ts`: since nothing inside a `.svelte` file has unit coverage, it is the only layer catching UI wiring. The flows it used to be the _sole_ coverage for — `applyAiEdit`, share-link import, profile switching — now live in `stores/trip.svelte.ts`, which vitest can import directly, so they are reachable by unit tests and should be tested there first; `trip.svelte.test.ts` still covers well under half of that file and is the widest coverage gap in the repo. What still has no reachable seam is the cloud-conflict resolution in `ui/tools/settings/SettingsPanel.svelte` (`landPulled`, `keepBothVersions`, `keepLocalVersion`, `stillActive`) — e2e covers three happy paths in `gdrive.spec.ts` and asserts none of the failure branches. It tests the built app via `vite preview`, hermetically — `fixtures.ts` aborts every non-localhost request, service workers are blocked (`serviceWorkers: "block"`, otherwise the PWA SW bypasses `page.route`), and tests seed `showmeway_user_yaml` with a far-future fixture (no `city`/`currency`, so weather/exchange never fire). The seed init-script only writes when the key is absent — it re-runs on `page.reload()`, and an unconditional write would wipe state the app just persisted.
- That abort is not a dead end for network-dependent features: a `page.route` registered inside a test outranks the context-level block, so mock there rather than loosening `fixtures.ts` — `chat.spec.ts` does it for Gemini's `/v1beta/models` and `/v1beta/interactions`. A models mock must return a single page: `listGeminiModels` follows `nextPageToken`, so a fixed response echoing one pages forever.
- Playwright runs a single project, `mobile-chromium` on `devices["Pixel 7"]` — no desktop and no WebKit project, so the Safari-specific workarounds in the code are untested. `smoke.spec.ts` and `today.spec.ts` narrow to 390px on purpose so the chip row overflows and the scroll-into-view assertions are not vacuous, and `contextOptions: { reducedMotion: "reduce" }` is load-bearing for those same tests — TabPager smooth-scrolls the active chip into view, so the geometry assertions would race without it. Do not drop either to chase a scroll flake.
- UI assertions use exact zh-TW strings incl. fullwidth punctuation (`｜`, `—`). Changing user-visible copy usually breaks several test files, not one: every spec asserts real strings through role/label locators, and validation-error text is asserted in `src/lib/domain/trip.test.ts` / `src/lib/infra/http/itinerary-loader.test.ts` as well — grep the whole repo for the old string.
- One suite deliberately depends on the real clock: `today.spec.ts` builds its trip around today's Asia/Taipei date (matching `timezoneId`) because `syncToToday` can only be exercised when a day actually is today — the far-future fixture leaves that path uncovered. Its day offsets are applied to that calendar date through UTC, never with `setDate`, or a runner whose own zone observes DST would collapse two days onto one Taipei date. It still uses the same network-blocking fixture, so only the dates are non-hermetic. Tests reach TabPager's chip row through `[data-pager-scroller]`, not through Tailwind class names.

## Git And Generated Files

- `dist/` is gitignored build output — never hand-edit it. `.github/workflows/deploy.yml` builds `main` and publishes it to GitHub Pages with `BASE_PATH=<repo name>`, so production is served from `/show-me-way/` while `pnpm dev`, `vite preview`, and the e2e suite all run at `/`. Fetch or link bundled `public/` assets **relatively** (`./itinerary.yaml`, as `fetchDefaultYamlText` does) — a leading slash passes every local check and 404s only on the deployed site.
- Do not commit personal itinerary data in `public/itinerary.local.yaml`.
- Avoid rewriting lockfiles unless dependencies or tool setup actually changed.
- Preserve unrelated user changes in the working tree.
- A `simple-git-hooks` pre-commit hook runs `lint-staged` (dprint on all staged files, `eslint --fix` on `*.{js,ts,svelte}`, and `schema:gen` when `src/lib/domain/trip-schema.ts` is staged — lint-staged stages the regenerated `schema/showmeway-schema.json` itself, so the task needs no `git add`). Expect staged files to be reformatted on commit.
